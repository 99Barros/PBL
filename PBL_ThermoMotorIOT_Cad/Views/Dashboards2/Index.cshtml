<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard 2</title>
    <link rel="stylesheet" href="/css/dashboard.css">
    <script src="https://cdn.canvasjs.com/canvasjs.min.js"></script>
    <script src="~/js/dashboard.js" defer></script>
</head>
<body>
    <div class="header">
        <h1>Dashboard - Total de Cadastros</h1>
    </div>
    <div class="dashboard">
        <div id="chartContainer"></div>
    </div>
    <script>
        // Dados do servidor passados via ViewBag
        const labels = @Html.Raw(Json.Serialize(ViewBag.Labels));
        const values = @Html.Raw(Json.Serialize(ViewBag.Values));

        // Prepara os dados para o gráfico
        const dataPoints = labels.map((label, index) => ({
            label: label,
            y: values[index]
        }));
    </script>
</body>
</html>
